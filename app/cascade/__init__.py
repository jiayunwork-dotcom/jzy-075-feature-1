"""级联链路：多段滤波器合成与链路级指标分析（独立处理路径）。

子模块划分：
- sections: 把每段分解为一阶 / 二阶实系数节，按节组织链路，逐段求根归因
- chain:    段解析（系数段 / 设计意图段）、等效系数卷积、稳定的链式求值
- metrics:  通带纹波、阻带衰减、相位线性度与群延时、稳定性归因
- service:  请求编排与业务校验
- schemas:  请求模型
- api:      HTTP 路由

数值原则：合成后的等效多项式阶数可达几十阶，直接对它求根或在单位圆上
代入求值都会被浮点误差毁掉（圆内极点被推到圆外、深阻带相消出假值）。
因此求根一律在"段"内进行，求值一律逐段进行再相乘；展开后的等效系数
只作为对外结果与复核输入，不作为内部计算的依据。
"""
